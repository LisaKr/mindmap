{
  "name": "japan-mindmap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check server.mjs",
    "test": "node --test",
    "preview": "node server.mjs"
  }
}